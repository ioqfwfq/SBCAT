# celltyping — waveform/firing cell-typing (build_labels.py + classify_celltypes.ipynb)
# Versions pinned to the shared E:\SBCAT\.venv (Python 3.13.3).
# Install:  ..\.venv\Scripts\python.exe -m pip install -r requirements.txt

numpy==2.4.4
scipy==1.17.1
pandas==2.3.3
matplotlib==3.10.9
seaborn==0.13.2
scikit-learn==1.8.0
pynwb==3.1.3
umap-learn==0.5.12
python-louvain==0.16
networkx==3.6.1
shap==0.52.0
xgboost==3.3.0

# the notebook (classify_celltypes.ipynb) also needs jupyterlab + ipykernel
# (already present in the shared .venv); build_labels.py runs without jupyter.
