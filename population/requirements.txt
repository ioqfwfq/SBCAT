# population — null-space / sequence / cell-selection geometry
# Versions pinned to the shared E:\SBCAT\.venv (Python 3.13.3).
# Install:  ..\.venv\Scripts\python.exe -m pip install -r requirements.txt

numpy==2.4.4
scipy==1.17.1
pandas==2.3.3
matplotlib==3.10.9
seaborn==0.13.2
scikit-learn==1.8.0
statsmodels==0.14.6
pynwb==3.1.3
tqdm==4.68.3

# imported but NOT in the current .venv (pip will install):
PyWavelets   # nwb_analysis/spectrogram.py -> import pywt

# notebooks require jupyterlab + ipykernel (already present in the shared .venv)
